/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['flagcdn.com', 'upload.wikimedia.org'], // Add all necessary domains here
  },
};

module.exports = nextConfig;
